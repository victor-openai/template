{
  "private": true,
  "name": "victor-template",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "type-check": "tsc --build",
    "release": "pnpm publish -r --access public"
    },
    "devDependencies": {
      "typescript": "^5.8.3",
      "rimraf": "^5.0.0",
      "@changesets/cli": "2.27.10"
    },
  "packageManager": "pnpm@10.11.0"
}
